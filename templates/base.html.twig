
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Default Title{% endblock %}</title>

    {% block stylesheets %}
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <!-- Bootstrap Icons -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
        <style>
            table {
                width: 100%;
                border-collapse: collapse;
            }
            th {
                background-color: #80cc28;
                color: white;
                padding: 12px;
                text-align: left;
                font-weight: 600;
                border-bottom: 2px solid #80cc28;
                position: sticky;
                top: 0;
            }
            td {
                padding: 10px 12px;
                border-bottom: 1px solid #dee2e6;
                vertical-align: middle;
            }
            tr:hover td {
                background-color: #f8f9fa;
            }
        </style>
    {% endblock %}

    {% block head %}{% endblock %}
</head>
<body>
{% block header %}
    <!-- Default header content -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <div class="main-logo">
                <a href="{{path('home')}}">
                    <img src="{{ asset('images/logo.png') }}" alt="logo" class="img-fluid" style="max-height: 60px; width: auto;">
                </a>
            </div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{path('home')}}" style="color: black">Accueil</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}{% endblock %}

{% block footer %}
    <!-- Default footer content -->
    <footer class="bg-light py-4 mt-5">
        <div class="container text-center">
            <p class="text-muted mb-0">© {{ "now"|date("Y") }} Digine. Tous droits réservés.</p>
        </div>
    </footer>
{% endblock %}

{% block javascripts %}
    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}
</body>
</html>